# Criamos uma lista de palavras - utilizando a linguagem Python

palavras = ["Programa Básico", "Programa de Variáveis", "Programa de Condicionais", "Programa de Simulação de App", "Programa com uso de While"]

# Inicializamos o índice com 0 > o que vai corresponder ao item 1 da listagem

indice = 2

# O laço while executa enquanto o índice for menor que o tamanho da lista

while indice < len(palavras):
    print(f"Palavra: {palavras[indice]}")  # Exibe a palavra atual
    indice += 1  # Incrementa o índice a cada iteração


# Esse aplicativo ainda não representa em si um programa utilizável (apenas uma proposta para a utilização de While)
